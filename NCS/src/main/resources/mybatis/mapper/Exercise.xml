<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etoile.app.DAO._Exercise">
<select id="drillListAll" resultType="com.etoile.app.DTO.Exercise">
	select a.dlid,b.did,b.name,b.comment,b.created,b.updated,b.dtype_id
	  from ncs_exercise a, ncs_drill b 
	 where a.course_id=#{cid} and a.drill_id=b.did
	 order by a.dlid
</select>
</mapper>